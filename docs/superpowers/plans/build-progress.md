# 10x Roadmap — Build Progress Tracker

> Durable state for the autonomous `/loop build everything` run. Each tick: read this,
> pick the next unchecked slice, build it (verify → browser-verify → commit → CI-green),
> tick the box, append a one-line log entry, reschedule. Order follows the roadmap's
> First Wave (§D), then impact×effort (§E). Roadmap: `2026-06-09-10x-value-roadmap.md`.

## Conventions
- One self-contained, committed, CI-green increment per tick.
- Large items are split into numbered slices; finish all slices before moving on.
- Keep every hard constraint (offline · no LLM · single-file · explainable · privacy).

## First Wave
- [x] **FIX-1 Ingestion robustness** — ✅ s1 header normalisation · ✅ s2 widened alias coverage (SAP/Workday/ADP) · ↪ s3 mapping report folded into BUILD-1
- [x] **BUILD-1 Column-mapping importer** — ✅ s1 engine · ✅ s2a parser override · ✅ s2b mapping UI + preview · ✅ s3 saved profiles (auto-apply on re-upload)
- [x] **BUILD-2 Action-tracking loop** — ✅ s1a model · ✅ s1b persistence + state · ✅ s2 UI (track from roadmap + status panel) · ✅ s3 newsletter + facts-pack integration
- [x] **UP-1 Longitudinal trends** — ✅ s1 timeseries builder · ✅ s2 sparkline component + KPI cards · ✅ s3 health-history chart
- [x] **UP-7 Flight-risk scoring** — ✅ s1/s2 already existed as `risk.ts` (per-employee explainable weighted score) · ✅ s3 added the **regrettable cohort** (top performers at Elevated+ risk → KPI + table + watchout in `risk.ts`) + a **`flight_risk_cohort` HR Brain finding**. Reconciled: removed a duplicate `flight_risk.ts` I'd started before finding `risk.ts`.
- [x] **BUILD-7 Benchmark pack** — ✅ s1 `benchmarkPacks.ts` (pack format + provenance, 4 illustrative built-ins, custom-pack parse/validate) · ✅ s2 Scorecard pack picker + provenance + custom-pack upload; `effectiveBenchmarks` flows to scorecard/Brain/newsletter; pack id + custom pack persist in the workspace
- [x] **BUILD-3 Manager/HRBP cockpit** — ✅ s1 `cockpit.ts` (manager/department scoping, team summary, risk scored-on-full-roster-then-filtered) · ✅ s2 Manager Cockpit page + nav (scope toggle, KPIs, watch-out flags, scoped risk table; browser-verified manager + multi-dept modes). **FIRST WAVE COMPLETE (7/7).**

## Then (impact × effort)
- [x] **UP-2 HR Brain rule expansion (+ per-dept mode)** — ✅ s1 `departmentScope.ts` (scope the whole store to one dept) · ✅ s2 HR Brain "Scope" selector (whole-org / per-dept; browser-verified Tech → 44/100, 12 findings vs org 27/100, 16) · ✅ s3 two new rules (span_of_control, leadership_representation)
- [x] **UP-3 Scenario Planner v2** — ✅ s1 `projectScenario` + `estimateMonthlyAttrition` (forward headcount/cost projection under attrition; run-off vs backfill) · ✅ s2 "Forward projection" section (horizon, attrition rate, backfill toggle, line chart, end KPIs; browser-verified backfill 135 flat + ₹1.26L vs run-off 135→129)
- [x] **UP-6 Equity & pay-equity depth** — ✅ pay quartiles by gender (glass-ceiling lens) + "Top-Quartile Women" KPI · like-for-like gap by job title (controlled, EU "category of workers") + "Roles > 5% Gap" KPI + per-role watchout · glass-ceiling watchout. Auto-renders on People>Pay Equity + newsletter; browser-verified on demo.
- [x] **BUILD-4 Compliance calendar** — ✅ s1 `compliance.ts` (statutory remittances + contract renewals → dated, classified overdue/due-soon/upcoming/settled) · ✅ s2 Compliance page + nav (summary chips, sorted open-obligations table with status badges, settled section; browser-verified 2 overdue/1 due-soon/7 upcoming/6 settled on demo)
- [x] **BUILD-5 Headcount planning** — ✅ s1 `headcount_plan.ts` (actual vs plan vs budget per dept: fill %, open-to-plan, over-plan, budget headroom, cost-vs-budget) · ✅ s2 demo plan data (+ re-embedded workspace) + Headcount Plan page/nav; browser-verified 135/143, cost −₹17.34L/mo on demo. (Schema `headcount_plan` already existed but was unused.)
- [x] **BUILD-6 Compensation module** — ✅ extended `buildCompensation` with the total-rewards dimension it ignored: Variable Pay Mix + Overtime Load KPIs, overtime-by-department chart, over-reliance watch-out (≥8 OT h/head). Auto-renders on People>Compensation + newsletter; browser-verified (15.0% / 16.3% on demo). (buildCompensation already covered distribution/dispersion/progression.)
- [x] **UP-4 Scorecard goals / off-track flags** — ✅ s1 `track` classification on ScorecardRow (on_track / at_risk = green-but-slipping / off_track) + summary counts · ✅ s2 "▾ at risk" summary chip + per-row marker; browser-verified "Avg Tenure (active)" flagged at-risk on demo.
- [x] **UP-5 Universal drill-down** — ✅ `MetricTable.drill` + clickable `DataTable` rows (cursor/hover/role/keyboard/aria) → onDrill; wired department tables (People dept/tenure/gender, pay-equity, compensation, cross-functional, headcount plan) + span→reporting_manager. Works wherever DomainView renders; browser-verified (Technology row → People 34/150).
- [x] **UP-8 Native PDF + comparison newsletter** — ✅ `comparison` block on the newsletter ("what changed since {prior}": health delta + new/resolved findings + improved/declined KPIs + at-risk) rendered on Reports + facts pack; browser-verified on demo. PDF = existing one-click "Print / Save as PDF" (window.print + print CSS — the offline/size-budget-respecting path; a bundled PDF lib would breach the 4MB/no-dep constraints).
- [x] **UP-9 White-label depth** — ✅ font theming (FontKey system/serif/humanist/mono, offline-safe system stacks, `safeFont` clamp, `--brand-font`) + Font selector on Branding; flows through export/import + persistence. Browser-verified live re-font. (Name/colours/logo/footer/theme/presets + brand-profile export-import already existed.)
- [x] **UP-10 Data lineage & audit** — ✅ `lineage.ts` (`buildLineage` → per-snapshot provenance: domain/period/as-of/source file/rows/compatibility + summary) + "Data lineage" section on Data Intake (full trail, below latest-per-domain "Loaded data"). Audit/activity log already existed. Browser-verified (23 snapshots / 13 domains on a live multi-month workspace).
- [ ] BUILD-8 Multi-entity rollup
- [ ] BUILD-9 Explore / pivot builder
- [ ] BUILD-10 Board-pack builder
- [ ] FIX-8 Performance & scale

## Quick wins (slot in opportunistically)
- [x] FIX-2b Branding-page overflow (minmax(0,1fr) so the form column shrinks; was +34px at 1024)
- [ ] DEMO-HIST Deepen demo to ~6 months of history (amplifies UP-1 sparklines + health chart; today the demo ships 2 periods so trends are 2-point lines). Touch `scripts/build-sample-workspace.mjs` to emit several prior months, then `npm run embed-demo`.
- [ ] FIX-2 Overflow regression guard
- [ ] FIX-3 Period-diff edge cases
- [ ] FIX-4 Bad-data resilience
- [ ] FIX-5 Print/PDF polish
- [ ] FIX-6 Accessibility deepening
- [ ] FIX-7 Demo realism (resolved finding)

## Log
- **UP-10 (complete)** — Data lineage (the activity/audit log already existed). `src/core/lineage.ts`: `buildLineage(snapshots, labelOf)` → one row per snapshot (domain, period, as-of, source file, rows, compatibility), sorted, + summary (snapshots/kinds/rows/period-range/full-partial). 4 tests. Data Intake "Data lineage" section: full provenance table + summary, between latest-per-domain "Loaded data" and the activity log. **Browser-verified** on a live multi-month workspace: 23 snapshots / 13 domains, source files + compatibility shown; no errors (verified aggregate structure only, no PII rows; code-only commit). 368 tests, build 2.84 MB. Next: BUILD-8 (multi-entity rollup).
- **UP-9 (complete)** — White-label font theming (name/colours/logo/footer/theme/presets + brand-profile export/import already existed). `branding.ts`: `font` (FontKey system/serif/humanist/mono) with FONT_STACKS of SYSTEM families only (no @font-face → never fetches a web font, honours offline); `safeFont` clamps untrusted input so `--brand-font` is always one of our constants (no injection); applyBranding sets it; theme.css base font reads `var(--brand-font)`. Branding page Font selector (each previewing its face); flows through export/import + persistence as part of Branding. **Browser-verified**: selecting Serif re-fonted the app live (body → Georgia stack); no app errors. 4 new tests. build 2.84 MB. Next: UP-10 (data lineage & audit).
- **UP-8 (complete)** — Period-comparison newsletter. `NewsletterComparison` + `comparison` field computed in `buildNewsletter` from the already-built brain + scorecard (health delta, new/resolved findings, improved/declined KPIs, at-risk) — null without a prior period; no recompute. Reports.tsx "What changed since {prior}" section (4-column diff + health line) + TOC entry; factsPack "## What Changed Since {prior}" block. PDF: the existing one-click "Print / Save as PDF" (window.print + print CSS) is the offline/single-file-respecting export — a bundled PDF lib would breach the 4MB/no-dep posture, so window.print() is deliberate. **Browser-verified**: "What changed since Apr 2026", HR Health 27/100 ▼−4 (was 31), 5 new findings, improved KPIs, Avg Tenure declined+at-risk; no errors. 1 new test. 361 tests, build 2.84 MB. Next: UP-9 (white-label depth).
- **UP-5 (complete)** — Universal drill-down. `MetricTable.drill` (optional field its first column maps to) + `DataTable` renders those rows clickable (cursor, hover, role=button, Enter/Space, aria-label) → onDrill; DomainView threads onDrill to every table + a caption hint. Wired `drill:"department"` into People department-breakdown/tenure/gender, pay-equity-by-dept, compensation-by-dept, cross-functional compound-risk, headcount plan-vs-actual; `reporting_manager` into span-of-control. Works wherever DomainView renders (People, Function Analytics, Cockpit, Headcount Plan). **Browser-verified**: Technology row → People filtered to 34/150 with a "Department: Technology" chip; no errors. 360 tests, build 2.83 MB. Next: UP-8 (native PDF + comparison newsletter).
- **UP-4 (complete)** — Scorecard goal trajectory. s1 `scorecard.ts`: `Track` (on_track / at_risk / off_track / none) on each ScorecardRow via `trackOf(rag, trendTone)` — at_risk = green but trending the wrong way (the future-red RAG misses); scorecardSummary gains offTrack/atRisk/onTrack. 4 tests. s2 Scorecard UI: "▾ N at risk" summary chip + per-row "· ▾ at risk" marker (amber). **Browser-verified**: demo shows "▾ 1 at risk" with "Avg Tenure (active)" flagged "On target · ▾ at risk" (green but −0.2yrs); no errors. 360 tests, build 2.83 MB. Next: UP-5 (universal drill-down).
- **BUILD-6 (complete)** — Compensation depth in `compensation.ts` (a DomainMetrics; `buildCompensation` already had distribution/dispersion/top-share/dept-medians/tenure-progression/compression). Added the variable + overtime fields it ignored: "Variable Pay Mix" KPI (variable_pay_paid % of gross), "Overtime Load" KPI (% with OT + avg hours), "Overtime hours by department" chart (drill), and an over-reliance watch-out (≥8 OT h/head, ≥15 = high). All gated on field presence. **Browser-verified** on demo: Variable Pay Mix 15.0%, Overtime Load 16.3%, OT chart present; no errors. 2 new tests. 356 tests, build 2.83 MB. Next: UP-4 (scorecard goals / off-track flags).
- **BUILD-5 (complete)** — Headcount planning (the `headcount_plan` schema existed but nothing consumed it). s1 `src/core/metrics/headcount_plan.ts`: `buildHeadcountPlan` (a DomainMetrics) joins actual active headcount to approved plan+budget per dept → Actual vs Plan + fill %, Open to Plan, Over Plan, Budget Headroom, Cost vs Budget (with payroll aggregate); variance chart (dept drill), plan-vs-actual table, under-plan/over-budget watch-outs; sub-departments roll up. 5 tests. s2: demoData emits a headcount_plan snapshot, MONTHLY set updated, sample workspace regenerated + re-embedded; new HeadcountPlan page + nav rendering via DomainView. **Browser-verified** on demo: 135/143 (94% filled), Open 10, Over 2, Headroom 20, Cost vs Budget −₹17.34L/mo, "Finance is under plan" watch-out; no errors. 354 tests, build 2.83 MB. Next: BUILD-6 (compensation module — note buildCompensation already exists; likely an enhancement).
- **BUILD-4 (complete)** — Compliance calendar. s1 `src/core/metrics/compliance.ts`: `buildComplianceCalendar` turns payroll_statutory (PF/ESI/PT/TDS/LWF) + admin_contract renewals into dated items classified overdue/due-soon(30d statutory, 60d contracts)/upcoming/settled, with signed days-until, owner, amount; sorted most-overdue-first. 6 tests. s2 `src/ui/pages/Compliance.tsx` + nav: summary chips, open-obligations table with coloured status badges + "when" + owner + amount, collapsible settled section; as-of = workspace period. **Browser-verified** on demo: 2 overdue/1 due-soon/7 upcoming/6 settled, sorted Acme Facilities 61d-overdue → due-today → upcoming; no app errors. 349 tests, build 2.82 MB. Next: BUILD-5 (headcount planning).
- **UP-6 (complete)** — Pay-equity depth in `pay_equity.ts` (a DomainMetrics, so it auto-renders on People>Pay Equity + the newsletter — no UI wiring): (1) pay quartiles by gender — rank workforce by pay into four bands, women's share each → "Women's share by pay quartile" chart + "Top-Quartile Women" KPI + a glass-ceiling watchout when the top band lags the workforce; (2) like-for-like gap by job_title (controlled "category of workers" comparison, role-mix held constant) → table + "Roles > 5% Gap" KPI + per-role watchout. **Browser-verified** on the demo: both KPIs, quartile chart, like-for-like table, and a "Like-for-like pay gap: Engineering Manager" watchout render; no console errors. 2 new tests. 343 tests, build 2.81 MB. Next: BUILD-4 (compliance calendar).
- **UP-3 (complete)** — Scenario Planner v2: forward attrition projection. s1 in `scenario.ts`: `projectScenario` (project headcount + run-rate cost over a horizon; run-off mode declines, backfill mode holds headcount flat and accrues recruitment cost — the recurring price of churn) + `estimateMonthlyAttrition` (data-derived default, clamped, with fallback). s2 Scenario page "Forward projection" section: horizon (6–36mo), monthly-attrition input (defaulted from data, resettable), backfill toggle, projected-headcount line chart, end KPIs. **Browser-verified**: backfill holds 135 + ₹1.26L cost at 0.4%/mo; run-off declines 135→129 and hides the backfill KPI; no app errors. 10 new tests. 341 tests, build 2.81 MB. Next: UP-6 (equity & pay-equity depth).
- **UP-2 (complete)** — HR Brain rule expansion + per-department mode. s1 `src/core/brain/departmentScope.ts`: `scopeStoreToDepartment` (re-projects every kind onto one dept by its natural key — dept field / employee_number / assigned-employee; ld_program & admin_contract kept whole; empty scopeable snapshots dropped) + `departmentsOf`. s2 HR Brain "Scope" selector (whole-org or any department) re-runs buildBrain + health-history on the dept slice; **browser-verified** Tech → 44/100, 12 findings vs org 27/100, 16 findings, with the scope note; no console errors. s3 two new diagnostic rules: `span_of_control` (3+ managers with 15+ reports → People>Managers) and `leadership_representation` (women 10pp+ under-represented in leadership → People>Representation) — both scope-aware. 10 new tests. 336 tests, build 2.81 MB. Next: UP-3 (Scenario Planner v2).
- **BUILD-3 (complete) — FIRST WAVE DONE (7/7)** — Manager/HRBP cockpit. s1 `src/core/metrics/cockpit.ts`: `managerOptions`/`departmentOptions`/`scopeEmployees` + `buildCockpit` (team headcount/relieved/pending-exits/new-joiners/avg-tenure, review completion, attrition-risk cohort scored on the FULL roster then filtered to scope so scores match other views, plain-language flags, top-N at-risk with named drivers). 8 tests. s2 `src/ui/pages/Cockpit.tsx` + AppShell nav ("Manager Cockpit"): scope toggle (by manager dropdown / by department chips, defaults to largest without effects), 6 KPI cards, watch-out flags, scoped risk table via KpiCard/DataTable, drill-to-People link. **Browser-verified**: manager mode (Imran Gupta, 24, "5 of 24 reviews pending"); department mode multi-select (Technology+Sales → "2 departments", 57); no app errors. Also fixed a real bug found en route: manual file Save/Load dropped tracked actions + benchmark pack (AppShell onSave/applyWorkspaceBytes) — now aligned with autosave. 326 tests, build 2.80 MB. Next (impact×effort): UP-2 HR Brain rule expansion.
- **BUILD-7 (complete)** — Benchmark packs. s1 `src/core/benchmarkPacks.ts`: `BenchmarkPack` (bands + provenance: source/year/region/industry/note/illustrative), 4 built-ins (general reuses DEFAULT_BENCHMARKS; India IT/ITeS, India BFSI, Global Tech — all labelled illustrative, encoding directional norms not survey figures), `effectiveBands` (pack over baseline, user edits on top), `parseBenchmarkPack` (validates a user JSON pack — requires name+source, numeric low≤high). s2: state holds `benchmarkPackId` + `customBenchmarkPack` + derived `effectiveBenchmarks` (consumed by Scorecard/Brain/newsletter); both persist in the workspace (additive). Scorecard gains a pack picker, a provenance line, and "Load custom pack…" (JSON upload → validated → attributed). **Browser-verified** (via JS-driven nav, since synthetic clicks weren't registering this session): picker shows all 4 packs + provenance + load button; switching to India IT changed the table bands live (Offer-Accept 80–90%→70–85%, L&D 60–85%→65–90%) and the provenance; only the benign extension messaging artifact in console, no app errors. 318 tests, build 2.79 MB. First Wave now 6/7 — only BUILD-3 (Manager/HRBP cockpit) remains. Next: BUILD-3.
- **UP-7 (reconciled, complete)** — Discovered the per-employee explainable attrition score UP-7 specifies ALREADY exists as `risk.ts` (people_risk: score/band/named contributors, renormalising weights, wired to Brain + newsletter + People risk tab). Rather than ship a duplicate, removed the `flight_risk.ts` I'd begun (s1/s2 commits 14fbf88/965fff5) and folded the new value in: (1) `risk.ts` — `EmployeeRisk.regrettable` (high performer at Elevated+), "Regrettable Risk" KPI, "Regrettable flight risk" table, watchout ≥3; purely additive (scores unchanged). (2) `brain.ts` — `flight_risk_cohort` rule firing on a material Elevated+ cohort (≥ max(5, 8% active)) or ≥3 regrettable; deep-links to People>risk. 5 new tests; cohort finding verified to fire on a realistic at-risk store + stay quiet on a stable one. 308 tests, build 2.78 MB. Browser tab-switch was flaky this tick (ref-clicks not registering, screenshots timing out) so the on-screen check was deferred — render path is existing, browser-verified components. Next: BUILD-7 (benchmark pack).
- **UP-1 s1** — `src/core/metrics/timeseries.ts`: pure period-series foundation — `periodList(store, kind?)`, `storeAsOf(store, asOf)` (reconstruct the workspace as of a date, snapshots carried forward), `buildSeries(store, valueAt, kind?)`, `compactSeries`. No Date.now. 6 tests. 293 tests, build 2.77 MB.
- **UP-1 s2** — KPI sparklines on every functional card. `MetricKPI.spark?`, pure `attachKpiSparklines(current, history)` (match by label across per-period recomputes, same-unit points, ≥2 to attach, preserves delta), wired into `buildDomainCompared` (dashboard + newsletter both pick it up), `sparklineGeometry` + `Sparkline.tsx` (accent-coloured, not green/red). **Browser-verified**: TA cards show flat mid-lines for unchanged KPIs + a rising line/dot for Offer-Accept (+6.3pp); 0 console errors. 301 tests, build 2.78 MB.
- **UP-1 s3** — `buildHealthHistory(store, opts)` recomputes HR Health at each roster month → a line ChartSpec; rendered on HR Brain under the score card via the existing Chart (themed). null <2 periods, capped at 24 months. **Browser-confirmed**: chart title+caption present in the live DOM, console clean (CDP screenshot timed out — tooling, not app). 303 tests, build 2.78 MB. **UP-1 COMPLETE.** Added DEMO-HIST quick win (demo ships 2 periods → 2-point lines; deepen for richer trends). Next: UP-7 (flight-risk scoring).
- **BUILD-2 s1a** — `src/core/actions.ts`: Action model (status open/in_progress/done, owner, due, findingId link) + pure helpers `actionSummary` (counts + overdue), `actionFromRoadmap`, `withStatus`, `hasOpenActionForFinding`. 4 tests. 283 tests, build 2.77 MB. Next: s1b workspace persistence + state threading.
- **BUILD-2 s1b** — `actions` now persists in the workspace (additive optional field, `saveWorkspace`/`loadWorkspace`, defaults []) and is threaded through `state.tsx` (state + `setActions`, restore on load, reset on demo→live and clearData, included in autosave). Round-trip test added. 284 tests, build 2.77 MB. Next: s2 actions UI panel on HR Brain (browser-verify).
- **BUILD-2 s2** — HR Brain: "+ Track" on each roadmap item creates a tracked action (deduped via `hasOpenActionForFinding`, shows "✓ Tracked"); a "Tracked actions" panel with per-row status select (Open/In progress/Done), due-date input, remove, and a live open/in-progress/done/overdue summary. **Browser-verified**: 16 track buttons on the demo; tracked an item → panel row + "1 open"; set Done → summary updated; 0 console errors. 284 tests, build 2.77 MB. Next: s3 newsletter integration (open actions + committed-vs-done in the board pack).
- **BUILD-2 s3** — newsletter + facts pack carry a "Tracked Actions" block (status summary + items), distinct from the watch-out action plan; `buildNewsletter` gains an `actions` option. Verified via model tests (trackedActions + summary) + typecheck; on-screen/print render is mechanical (Playwright was disconnected this tick, so the visual check is deferred — the action-creating UI was browser-verified in s2). 287 tests, build 2.77 MB. **BUILD-2 COMPLETE.** Next: UP-1 (longitudinal trends).
- **FIX-1 s1** — `normalizeHeader()` (lower-case + unify `_-./` separators + collapse whitespace) applied to both alias-map keys and incoming headers, so real-world export drift matches. New header-drift test + existing Keka round-trip both green. 270 tests, build 2.76 MB.
- **FIX-1 s2** — widened EMPLOYEE_ALIASES with SAP/Workday/ADP synonyms (Staff ID / Personnel Number, Hire Date, Title, Work Location, Company, Sex, Dept, …). New alias test green; FIX-1 complete. 271 tests, build 2.76 MB. Next: BUILD-1 (column-mapping importer).
- **BUILD-1 s1** — `src/core/ingest/mapping.ts`: `suggestColumnMapping` (auto-suggest header→field + unmapped/missing-required gaps) and `validateColumnMapping` (ambiguous/invalid mapping detection). Pure + deterministic, 5 tests. 276 tests, build 2.76 MB. Next: s2 mapping UI + preview (needs browser-verify).
- **BUILD-1 s2a** — `parseWorkbook` now returns `detectedHeaders` (surfaced even on a near-miss) and accepts a `mappingOverride` (header→field|null) that wins over alias detection and bypasses the score threshold. 1 test (rejected→headers surfaced; override→imports). 277 tests, build 2.76 MB. Next: s2b mapping UI (browser-verify).
- **BUILD-1 s2b** — Data Intake gains an "Adjust column mapping" editor: holds the uploaded bytes, shows each detected header → a field dropdown (pre-filled from `suggestColumnMapping`), re-parses live on change, flags unmapped required fields. **Browser-verified**: uploaded a messy-header file (EmpCode/PersonName/JoinDt/Town) → auto-rejected → editor appeared → mapped EmpCode→Employee Number + set as-of → flipped to "Ready to import" (E1, 2 rows). No app console errors. 277 tests, build 2.77 MB. Next: s3 saved mapping profiles.
- **BUILD-1 s3** — `src/core/ingest/mappingProfiles.ts`: localStorage-backed saved mappings keyed by domain + header shape, with pure `profileApplies` / `resolveProfileForFile` (drift-tolerant re-keying). Data Intake auto-applies a matching profile on upload (with a note) and saves the mapping on import. **Browser-verified round-trip**: map a messy file → import (profile saved) → re-upload the same file → mapping auto-applied, "Ready to import" with zero manual steps. 0 console errors. 279 tests, build 2.77 MB. **BUILD-1 COMPLETE.** Profiles are per-device (localStorage); could later migrate to the workspace file. Next: BUILD-2 (action-tracking loop).
